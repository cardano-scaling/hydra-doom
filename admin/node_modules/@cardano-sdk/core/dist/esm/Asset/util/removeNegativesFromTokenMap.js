export const removeNegativesFromTokenMap = (assets) => {
    const result = new Map(assets);
    for (const [assetId, assetQuantity] of result) {
        if (assetQuantity < 0) {
            result.delete(assetId);
        }
    }
    return result;
};
//# sourceMappingURL=removeNegativesFromTokenMap.js.map