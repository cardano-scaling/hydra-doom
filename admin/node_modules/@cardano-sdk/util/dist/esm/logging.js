export const loggerMethodNames = ['debug', 'error', 'fatal', 'info', 'trace', 'warn'];
export const contextLogger = (logger, context, contextStringify = (ctx) => (logger.contextStringify ? logger.contextStringify(ctx) : `[${ctx.join('|')}]`)) => {
    const derivedLogger = {
        baseLogger: logger.baseLogger || logger,
        contextArray: [...(logger.contextArray || []), context],
        contextStringify
    };
    const contextStr = contextStringify(derivedLogger.contextArray);
    return { ...derivedLogger, ...Object.fromEntries(loggerMethodNames.map((method) => [
            method,
            (message, ...optionalParams) => {
                derivedLogger.baseLogger[method](contextStr, message, ...optionalParams);
            }
        ])) };
};
//# sourceMappingURL=logging.js.map