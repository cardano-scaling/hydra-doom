/// <reference types="ws" />
import IsoWebSocket from 'isomorphic-ws';
export declare class WebSocket extends IsoWebSocket {
    constructor(addr: string | URL, opts?: IsoWebSocket.ClientOptions);
}
export interface CloseEvent {
    wasClean: boolean;
    code: number;
    reason: string;
    type: string;
    target: IsoWebSocket;
}
//# sourceMappingURL=IsomorphicWebSocket.d.ts.map